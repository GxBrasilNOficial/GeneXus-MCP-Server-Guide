# Autorização de Trabalho — GeneXus MCP Tests

**Validade:** Este arquivo autoriza Claude a executar operações de teste e validação na KB `NextTest1` e workspace `C:\Dev\Test\NextTest1` SEM solicitar aprovação a cada ação.

---

## ✅ AUTORIZADO — Executar diretamente

### MCP Server — Operações de KB
- `open_knowledge_base` / `close_knowledge_base`
- `export_kb_to_text` / `import_text_to_kb` / `validate_kb_text_files`
- `build_one` / `build_all` / `compile_object`
- `get_kb_property` / `set_kb_property` / `reset_kb_property`
- `export_knowledge_manager` / `import_knowledge_manager`

### Filesystem — Leitura e escrita
- Criar/editar/validar arquivos em `C:\Dev\Test\NextTest1/**`
- Criar/editar/validar arquivos em `C:\Dev\Knowledge\GeneXus-MCP-Server-Guide/**`
- Ler logs: `GXMBLServices.log`, arquivos `.ari`, `.dll` timestamps
- Monitorar build output via SSE/progressToken

### Processos — Controle local
- Listar processos dotnet (`Get-Process`, `Get-WmiObject`)
- Parar web app (PID com `GxNetCoreStartup.dll --urls http://localhost:5000`)
- Parar daemons GeneXus órfãos (`SpecifierDaemon`, `GeneratorDaemon`, `ResidentBuilderDaemon`)
- Executar scripts PowerShell em `C:\Dev\Test\NextTest1/` (find_lock.ps1, check_lock.ps1, kill_gx_daemons.ps1)

### Web — Testes de aplicação
- Navegar para `http://localhost:5000/<objeto>.aspx`
- Verificar/validar conteúdo visual de páginas
- Confirmar mudanças após import/build

### Documentação — Atualizar registros
- Editar `mcp-tools.md` — adicionar descobertas, novos itens na lista de armadilhas
- Editar `README.md` — mover itens entre "Pontos em aberto" e "Validações concluídas"
- Criar/editar arquivos de memória em `.claude/projects/.../memory/`

---

## ⚠️ REQUER CONFIRMAÇÃO PRÉVIA — Antes de executar

### Operações destrutivas
- `create_or_impact_database` — SEMPRE perguntar antes (pode deletar dados)
- Qualquer operação que delete objetos da KB (excluir arquivos `.gx` com intenção)
- Renomear/mover repositórios ou pastas principais

### Operações fora do escopo local
- Abrir/fechar a IDE GeneXus
- Modificar configurações globais (`~/.npmrc`, `settings.json` não-local)
- Enviar dados para servidores externos (pastebin, gist, etc)

---

## 🚫 NÃO AUTORIZADO

- Deletar objetos da KB via MCP (não existe ferramenta, mas se houver, não use)
- Modificar KB que não seja `NextTest1`
- Acessar credenciais ou arquivos sensíveis fora do escopo autorizado
- Fazer commits sem aprovação explícita do usuário

---

## Como este arquivo funciona

1. Claude lê este arquivo no início do trabalho
2. Claude executa operações autorizadas SEM pedir aprovação
3. Claude apenas notifica/pergunta para operações em ⚠️
4. Se Claude encontrar uma operação que deveria estar aqui mas não está, avisa e atualiza este arquivo
5. O usuário pode adicionar/remover itens a qualquer momento — Claude re-lê na próxima execução

---

**Assinado:** 2026-04-10 21:05:30 -03:00
